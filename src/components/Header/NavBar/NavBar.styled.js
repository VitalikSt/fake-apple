import styled from 'styled-components';

export const Nav = styled.nav `
	ul {
		display: flex;
		padding: 0;
		list-style: none;
		li {
			a {
				text-decoration: none;
				color: #fff;
				font-size: 12px;
				font-weight: 700;
				line-height: 19px;
			}
		}
	}
`;
